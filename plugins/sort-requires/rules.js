module.exports = {
  /**
   * Enforce alphabetically sorting of variable declarations in a group of
   * require() calls. A group is a section of code where there are no blank
   * lines between the end of one variable declaration node with a require() call
   * the beginning of the next.
   */
  "sort-requires/sort-requires": "error",
}
